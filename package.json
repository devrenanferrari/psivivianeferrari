{
  "name": "psivivianeferrari",
  "private": true,
  "description": "Site + API de agendamento da psicóloga VF (Node + PostgreSQL)",
  "engines": { "node": ">=18" },
  "scripts": { "start": "node server/server.js" },
  "dependencies": {
    "nodemailer": "^10.0.0",
    "pg": "^8.13.1"
  }
}
